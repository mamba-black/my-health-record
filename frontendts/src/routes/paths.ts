import { resolve } from "$app/paths";

export const ROOT = resolve(`/`);
export const LOGIN = resolve(`/login`);
export const CALLBACK = resolve(`/auth/google/callback`);
export const SEARCH = resolve(`/search`);
